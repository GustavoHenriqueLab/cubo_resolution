[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cubo"
version = "0.1.0"
dependencies = [
    "selenium",
    "python-dotenv",
    "google-genai",
]

[tool.setuptools.packages.find]
where = ["src"]


